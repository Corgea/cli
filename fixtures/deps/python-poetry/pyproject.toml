[project]
name = "deps-fixture-poetry"
version = "0.1.0"
description = "Sample app for corgea deps poetry.lock dogfood."
requires-python = ">=3.8"
dependencies = [
    "django==2.2.0",
    "pyyaml==5.1",
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
