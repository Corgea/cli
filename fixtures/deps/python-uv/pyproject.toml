[project]
name = "deps-fixture-uv"
version = "0.1.0"
description = "Sample app for corgea deps uv.lock dogfood."
requires-python = ">=3.8"
dependencies = [
    "django==2.2.0",
    "urllib3==1.25.8",
]
