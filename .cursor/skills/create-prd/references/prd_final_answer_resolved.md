### PRD Complete

The PRD is complete at the verified artifact path.

In an interactive run, offer to continue with `/create-tdd` using the same task directory or to refine one specific product decision. Ask only one question.

In an orchestrated no-user run, do not offer next steps. Return the absolute PRD path, mockup paths, complete decision ledger, unresolved root questions, and verification in the parent's required envelope.
