## Outline Updated

The canonical structure outline was updated in place.

Report its absolute path, phase order, exact owned paths, exact automated commands, manual verification steps, open questions, changed artifact paths, and resulting content hash. Do not recommend creating another checkout or launching another implementation orchestrator.

In an interactive run, ask the user to review the phase boundaries and verification steps. In a parent no-user run, return the parent's required envelope without prompting the user.
