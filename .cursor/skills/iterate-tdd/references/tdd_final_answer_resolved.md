### TDD Updated

The canonical TDD was updated in place at the verified artifact path.

In an interactive run, state the one resolved change and ask what technical decision to address next. Ask only one question.

In an orchestrated no-user run, do not prompt the user. Return the absolute TDD path, changed diagram paths, revised decision-ledger entries, unresolved root questions, and verification in the parent's required envelope.
