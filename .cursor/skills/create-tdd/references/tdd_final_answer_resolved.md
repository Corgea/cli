### TDD Complete

The TDD is complete at the verified artifact path.

In an interactive run, offer to continue with `/create-structure-outline` using the same task directory or to refine one specific technical decision. Ask only one question.

In an orchestrated no-user run, do not offer next steps. Return the absolute TDD path, diagram paths, complete decision ledger, unresolved root questions, and verification in the parent's required envelope.
