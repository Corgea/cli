//! Python lockfile parsing — shared-parser module boundary placeholder.

#![allow(dead_code)]

use std::path::Path;

/// Placeholder for the shared Python lockfile parser (not yet extracted).
pub fn parse_poetry_lock(_path: &Path) -> Result<(), String> {
    unimplemented!("deps::parse::python_lock")
}
